{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": [
    "main",
    "global-lookup"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
